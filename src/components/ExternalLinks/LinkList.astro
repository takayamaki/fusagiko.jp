---
export interface Props {
  links: {
    name: string;
    icon: string;
    href: string;
  }[];
}

import Container from "../Continer.astro";
import Link from "./Link.astro";

const { links } = Astro.props;
---

<Container>
  {links.map((link: Props["links"][number]) => <Link link={link} />)}
</Container>
