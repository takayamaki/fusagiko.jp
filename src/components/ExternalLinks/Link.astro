---
export interface Props {
  link: {
    name: string;
    icon: string;
    href: string;
  };
}

const { name, icon, href } = Astro.props.link;
---

<style>
  i,
  span {
    font-size: 2rem;
  }
</style>

<div>
  <a href={href}>
    <i class={icon}></i>
    <span>{name}</span>
  </a>
</div>
